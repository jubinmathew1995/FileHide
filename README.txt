*********FileHide************

This application is completely build in python. And the GUI elements are included using Tkinter module.

This application hides a given application like pdf,images ,videos etc into another application like pdf,images etc.